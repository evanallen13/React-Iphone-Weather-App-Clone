import React, { Component } from 'react';
import './footer.css'

const Footer = (props) =>{
    return(
        <div className='bullerContainer'>
            {/* <button
                onClick={() => 
                    document.location.href = "/"
                }
            >Menu</button> */}
        </div>
    )
}

export default Footer;